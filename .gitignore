node_modules
dist
*.local
.DS_Store

# ingest pipeline data (sqlite + jsonl exports)
ingest/data/

# secrets — never commit; each host keeps its own (.env.example is fine)
.env
*.env


# python venv
.venv/
